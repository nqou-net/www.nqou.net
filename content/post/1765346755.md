---
title: "GitHub Copilot エージェントパネル入門 - 第9回: 実践パターン集（コードレビュー自動化）"
description: "プロダクション品質のコードレビューエージェントの実装。PRへの自動コメント、レビュー品質向上、チーム運用のベストプラクティスまで"
date: 2025-12-10T15:00:00+09:00
draft: true
tags:
  - GitHub
  - Copilot
  - Agent Panel
  - コードレビュー
  - 自動化
image: /favicon.png
---

## はじめに

[@nqounet](https://x.com/nqounet)です。

第9回では、実践パターン集の第1弾として、コードレビュー自動化の完全な実装を学びます。プロダクション環境で使える本格的なエージェントを構築しましょう。

## 完全なコードレビューエージェント

### エージェント構成

```yaml
---
name: production-code-reviewer
description: プロダクション品質のコードレビューエージェント
version: 3.0.0
author: Development Team

parameters:
  - name: reviewLevel
    type: string
    default: "standard"
    options:
      - "quick"      # 基本チェックのみ
      - "standard"   # 標準的なレビュー
      - "thorough"   # 徹底的なレビュー
  
  - name: language
    type: string
    required: true
    options: ["javascript", "typescript", "python", "java", "go"]
  
  - name: autoComment
    type: boolean
    default: true
    description: "PRに自動でコメントを投稿"
  
  - name: blockOnCritical
    type: boolean
    default: true
    description: "Critical問題がある場合マージをブロック"

dependencies:
  agents:
    - name: syntax-checker
      version: "^2.0.0"
    - name: security-scanner
      version: "^3.1.0"
    - name: performance-analyzer
      version: "^1.5.0"

env:
  - name: GITHUB_TOKEN
    required: true
    type: secret
  - name: OPENAI_API_KEY
    required: true
    type: secret

workflow:
  type: hybrid
  
  # Phase 1: 準備
  preparation:
    - agent: pr-context-fetcher
      output: pr_context
    
    - agent: file-collector
      input: "${pr_context}"
      output: changed_files
  
  # Phase 2: 並列分析
  analysis:
    type: parallel
    maxConcurrency: 5
    steps:
      - agent: syntax-checker
        input: "${changed_files}"
        output: syntax_issues
      
      - agent: lint-runner
        input: "${changed_files}"
        output: lint_issues
      
      - agent: security-scanner
        input: "${changed_files}"
        output: security_issues
      
      - agent: performance-analyzer
        input: "${changed_files}"
        output: performance_issues
      
      - agent: best-practices-checker
        input: "${changed_files}"
        output: best_practices_issues
  
  # Phase 3: AI レビュー
  ai_review:
    - agent: ai-code-reviewer
      input:
        files: "${changed_files}"
        context: "${pr_context}"
        previousIssues:
          - "${syntax_issues}"
          - "${lint_issues}"
          - "${security_issues}"
      output: ai_review_result
  
  # Phase 4: レポート生成とアクション
  reporting:
    - agent: review-aggregator
      input:
        syntax: "${syntax_issues}"
        lint: "${lint_issues}"
        security: "${security_issues}"
        performance: "${performance_issues}"
        bestPractices: "${best_practices_issues}"
        aiReview: "${ai_review_result}"
      output: final_review
    
    - condition:
        if: "${parameters.autoComment} == true"
        then:
          - agent: github-commenter
            input: "${final_review}"
            parameters:
              prNumber: "${pr_context.number}"
    
    - agent: status-updater
      input: "${final_review}"
      parameters:
        block: "${parameters.blockOnCritical}"
---

# Production Code Reviewer

あなたは経験豊富なシニアソフトウェアエンジニアです。
高品質なコードレビューを提供し、チームの生産性とコード品質を向上させます。

## レビュー観点

### 1. コードの正確性
- ロジックエラーの検出
- エッジケースの考慮
- エラーハンドリングの適切性

### 2. 保守性
- コードの可読性
- 命名規則の遵守
- 適切なコメント

### 3. パフォーマンス
- アルゴリズムの効率性
- メモリ使用量
- 不要な処理の削減

### 4. セキュリティ
- 脆弱性の検出
- 入力検証
- 認証・認可の確認

### 5. テスト
- テストカバレッジ
- テストの品質
- エッジケースのテスト

## レビューレベル別の動作

### Quick モード
- 構文エラーとセキュリティの Critical 問題のみ
- 実行時間: ~5秒

### Standard モード
- 上記 + Lint、ベストプラクティス
- 実行時間: ~30秒

### Thorough モード
- 全チェック + AIによる詳細分析
- 実行時間: ~2分

## 出力形式

### PRコメント形式
\`\`\`markdown
## 🤖 自動コードレビュー結果

### 📊 サマリー
- **総合評価**: B+ (85/100)
- **Critical問題**: 0
- **Warning**: 3
- **Info**: 5

### 🔴 Critical Issues
なし

### 🟡 Warnings

#### src/auth.ts:42
\`\`\`typescript
// ❌ Before
const token = req.headers.authorization;
\`\`\`

**問題**: Authorization ヘッダーの検証が不十分
**影響**: 不正なトークンで認証される可能性
**推奨**:
\`\`\`typescript
// ✅ After
const authHeader = req.headers.authorization;
if (!authHeader || !authHeader.startsWith('Bearer ')) {
  throw new UnauthorizedError('Invalid authorization header');
}
const token = authHeader.slice(7);
\`\`\`

### ✅ Good Practices Found
- 適切な型定義
- エラーハンドリングの実装
- テストカバレッジ90%

### 💡 Suggestions
1. `src/utils.ts` のユーティリティ関数をユニットテストで検証
2. `README.md` に使用例を追加
\`\`\`
```

## AI レビューエージェントの実装

```markdown
---
name: ai-code-reviewer
version: 2.0.0
---

# AI Code Reviewer

## システムプロンプト

あなたは10年以上の経験を持つシニアソフトウェアエンジニアです。
以下の原則に基づいてコードをレビューしてください：

### レビュー原則
1. **建設的**: 批判ではなく改善提案
2. **具体的**: 抽象的な指摘ではなく、具体的なコード例
3. **バランス**: 問題点だけでなく良い点も指摘
4. **教育的**: なぜその変更が必要か説明

### レビュープロセス

#### Step 1: コードの理解
\`\`\`
思考: このPRは何を実現しようとしているのか？
- 目的: ${pr_context.title}
- 説明: ${pr_context.description}
- 変更ファイル数: ${changed_files.length}
\`\`\`

#### Step 2: 各ファイルの分析
\`\`\`
For each file in ${changed_files}:
  1. 変更の意図を理解
  2. 潜在的な問題を特定
  3. 改善案を検討
  4. 重要度を判定 (Critical/Warning/Info)
\`\`\`

#### Step 3: 総合評価
\`\`\`
思考: 全体としてこのPRは...
- コード品質: ${quality_score}/100
- テストの充実度: ${test_score}/100
- ドキュメント: ${doc_score}/100
\`\`\`

## 出力テンプレート

### ファイル単位のレビュー
\`\`\`markdown
#### ${file.path}

**変更の概要**: ${file.summary}

**問題点**:
1. Line ${line}: ${issue}
   - 影響: ${impact}
   - 推奨: ${suggestion}

**良い点**:
- ${good_practice}

**スコア**: ${score}/100
\`\`\`

### 全体レビュー
\`\`\`markdown
## 総合評価

**スコア**: ${overall_score}/100

**主な改善点**:
1. ${improvement_1}
2. ${improvement_2}

**推奨アクション**:
- [ ] ${action_1}
- [ ] ${action_2}
\`\`\`
```

## GitHub統合の実装

### PRへの自動コメント

```typescript
// github-commenter.agent.ts
interface ReviewComment {
  path: string;
  line: number;
  side: 'LEFT' | 'RIGHT';
  body: string;
}

async function postReviewComments(
  prNumber: number,
  comments: ReviewComment[]
) {
  // インラインコメント
  for (const comment of comments) {
    await octokit.pulls.createReviewComment({
      owner,
      repo,
      pull_number: prNumber,
      path: comment.path,
      line: comment.line,
      side: comment.side,
      body: comment.body
    });
  }
  
  // サマリーコメント
  await octokit.pulls.createReview({
    owner,
    repo,
    pull_number: prNumber,
    event: 'COMMENT',
    body: generateSummary(comments)
  });
}
```

### ステータスチェックの更新

```typescript
async function updateCommitStatus(
  sha: string,
  reviewResult: ReviewResult
) {
  const state = reviewResult.criticalIssues > 0 
    ? 'failure' 
    : reviewResult.warnings > 0 
    ? 'success'  // Warning は成功扱い
    : 'success';
  
  await octokit.repos.createCommitStatus({
    owner,
    repo,
    sha,
    state,
    context: 'copilot-agent/code-review',
    description: `${reviewResult.criticalIssues} critical, ${reviewResult.warnings} warnings`,
    target_url: reviewResult.reportUrl
  });
}
```

## レビュー品質の向上

### コンテキストの活用

```markdown
## コンテキスト情報の収集

### PRメタデータ
- タイトル、説明
- 関連Issue
- ラベル

### 過去の履歴
- 同じファイルの過去の問題
- レビュアーのフィードバック傾向

### プロジェクト情報
- コーディング規約
- アーキテクチャガイドライン
- 過去のベストプラクティス

## コンテキストを活用したレビュー

\`\`\`
思考: このPRは Issue #${issue_number} に関連
Issue の内容: "ログイン機能のバグ修正"

→ セキュリティ観点を重点的にチェック
→ 認証まわりのテストを確認
→ エッジケース（空パスワード等）の処理を検証
\`\`\`
```

### 学習と改善

```yaml
learning:
  enabled: true
  feedback:
    # レビューへの反応を学習
    - type: "comment-resolved"
      weight: 1.0
    - type: "comment-disputed"
      weight: -0.5
  
  adaptation:
    # チームの傾向に適応
    - metric: "review-acceptance-rate"
      target: ">80%"
    - metric: "false-positive-rate"
      target: "<10%"
```

## チーム運用のベストプラクティス

### レビュールールの設定

```yaml
# .github/copilot-agent-rules.yml
rules:
  # 必須チェック
  required:
    - syntax-check
    - security-scan
    - test-coverage
  
  # プロジェクト固有ルール
  custom:
    - name: "no-console-log"
      pattern: "console\\.log"
      message: "本番コードでは logger を使用してください"
      severity: "warning"
    
    - name: "require-jsdoc"
      pattern: "^(export )?(function|class)"
      requiresDoc: true
      severity: "info"
  
  # 自動修正
  autofix:
    - name: "prettier-format"
      enabled: true
    - name: "import-sort"
      enabled: true
```

### 段階的な導入

```markdown
## Phase 1: 監視モード（1-2週間）
- コメントは投稿するがマージはブロックしない
- チームのフィードバックを収集
- 誤検知のパターンを学習

## Phase 2: 段階的な適用（2-4週間）
- Warning 以上でマージをブロック
- 特定のファイルパターンのみ適用

## Phase 3: 全面展開
- すべてのPRに適用
- Critical 問題でマージブロック
```

## 動作確認チェックリスト

- [ ] コードレビューエージェントを実装した
- [ ] PRに自動コメントを投稿できた
- [ ] ステータスチェックを更新した
- [ ] AIレビューを統合した
- [ ] チームルールを設定した
- [ ] 段階的導入計画を作成した

## 次回予告

第10回では、ドキュメント生成の実践パターンを学びます：

- READMEの自動生成
- APIドキュメント作成
- コードコメントの自動追加
- 変更履歴の自動作成

## まとめ

この記事では、コードレビュー自動化の実践パターンを学びました：

1. **完全なレビューエージェント**
   - 並列分析パイプライン
   - AIレビューの統合

2. **GitHub統合**
   - PR自動コメント
   - ステータスチェック

3. **品質向上**
   - コンテキストの活用
   - 学習と改善

4. **チーム運用**
   - カスタムルール設定
   - 段階的な導入

次回は、ドキュメント生成を自動化し、コードと常に同期した高品質なドキュメントを維持する方法を学びます！
